# guacd mit VA-API Support für AMD Ryzen (Radeon)
FROM guacamole/guacd:1.6.0

USER root

# VA-API Hardware-Encoding Treiber für AMD (Alpine Linux)
RUN apk add --no-cache \
    mesa-va-gallium \
    libva-intel-driver \
    libva \
    libva-utils

USER guacd
